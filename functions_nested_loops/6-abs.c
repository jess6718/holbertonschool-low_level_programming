#include "main.h"

/**
 * _abs - absolute
 * @n: input int
 * Description: prints absolute value of the number
 * Return: absolute value
 */

int _abs(int n)
{

	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
